import React from 'react'
import Animal from './animal'

function Animalss({name, image}) {
    return(
            Animal.map((item,index)=> {item.name})
    )
}

export default function Animals(Animalss) {
    return(
            Animalss
    )
}

